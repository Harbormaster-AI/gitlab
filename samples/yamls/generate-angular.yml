app:
    - techStackId:       Angular7MongoDB
      modelId:           4                                                     # id or file; ignored when provided via cmd line arg
      appOptionsFile:    ./samples/options/Angular7MongoDB.options.json # ignored when provided via cmd line arg
      gitParams:
         entry:          GitLab_Test_Angular7MongoDB
         file:           ./samples/git/gitlab.yml                            # ignored when provided via cmd line arg
      saveParams:
         name:           Angular7 MongoDB v0.0.1
         description:    Angular7 Test Application Generation
